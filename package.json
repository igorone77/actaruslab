{
  "name": "model-autopsy-ui",
  "private": true,
  "version": "0.1.0",
  "description": "NEUTRA front end for the MODEL AUTOPSY engine. Built to web/static/, which autopsy.api serves.",
  "type": "module",
  "scripts": {
    "build": "node web/build.mjs",
    "watch": "node web/build.mjs --watch"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
